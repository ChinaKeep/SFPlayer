# Library Modules

This repository exposes SFPlayer as componentized modules through CocoaPods subspecs:

- `SFPlayer/UI`
- `SFPlayer/Core`
- `SFPlayer/Container`

Actual source files are currently located in `SFPlayer/SFVideoPlayer` and mapped by `SFPlayer.podspec`.
